#[PATHS]
#ti_dir = ../../data/time_intervals
# name of directory with generated time interval dirs.
# if == 0 then it will be generated as time string at the moment of generating
#case_dir = my_borders_1try


[DATASET]
dataset_name = TandP

[TI]
ti = my_borders_1try

[MODULES]
gear_module = gear_ones
estimator_module = procents
reporter_module = simple_print

[SETTINGS]
predict = true
estimate = true
